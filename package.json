{
  "scripts": {
    "quiz:build": "node moodle/gift2md.mjs",
    "docs:dev": "vitepress dev",
    "docs:build": "npm run quiz:build && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  },
  "overrides": {
    "vite": "^6.4.3",
    "esbuild": "^0.25.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
